# SW-007 closeout evidence

`integration/sw007-api-closeout.json` is generated by
`pnpm acceptance:sw007:api` while the real Fastify API and Python matcher are
running. It uses only the visible `organizations-dev-v1` CSVs for product input.

The retained run confirms the unchanged SW-006 development bands (11 matched,
8 Needs Review, 4 Only A, 11 Only B), a two-candidate ambiguous case, reversible
defer, human SAME with separately created unresolved conflicts, eligible undo,
DIFFERENT with no conflicts, process-local refresh recovery, and unchanged source
SHA-256 hashes.

This report is API/product-state integration evidence, not a browser walkthrough or
matcher benchmark. Browser screenshots are intentionally absent: the required
in-app browser control surface could not connect during this closeout. Component
tests cover the visible keyboard, focus, alternatives, collision, accessibility,
and virtualization behavior, but they do not substitute for the outstanding real
visible-product walkthrough.
