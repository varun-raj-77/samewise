# Benchmarks

Versioned benchmark configurations and manifests record generator/config versions, requested and actual counts, filenames, and content hashes. Do not add matcher claims without executable evaluation and traceable data.
