# Shared configuration

This package directory is reserved for configuration shared across Samewise workspace projects. Tool-specific configuration remains beside its project or at the repository root until a concrete shared need exists.
