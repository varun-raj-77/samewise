# One-minute Samewise demo

**Target:** 56 seconds of product-only screen recording at a consistent desktop viewport. Use deterministic visible synthetic CSVs. Record cursor actions without browser chrome, private tabs, personal data, hidden truth files, or API keys. Narration can be recorded separately.

| Time | Screen and action | Narration / optional caption | Cut |
| --- | --- | --- | --- |
| 0–4 s | On Upload, click **Try sample data**; cut as the two synthetic vendor profiles appear. | “Start immediately with a small synthetic vendor pair.” | Keep sample onboarding to one click and a few seconds. |
| 4–13 s | Match setup: confirm the corresponding columns and show **Use to match**, **Keep in result**, and semantic family. | “Corresponding fields establish identity separately from the values kept in the result.” | The same manual fallback works without OpenAI. |
| 13–20 s | Run matching; show the result summary with automatic, review, and source-only outcomes. | “Clear records match automatically; ambiguous evidence is held for review.” | Cut directly into review. |
| 20–33 s | Open Orchard Signal Creative. Hold on the similar name and address, missing stable ID, contact typo, and contradictory email and phone; choose **Same entity**. | “The reviewer sees exactly why Samewise abstained, then decides identity.” | Use the real matcher evidence; do not inject a result. |
| 33–47 s | Continue to **Merge values**; show company, address, contact, and updated-date differences, preview a deterministic rule, then explicitly apply it. | “Only after identity is settled do deterministic merge rules handle value differences.” | Resolve any remaining exceptions in the prepared run before the export cut. |
| 47–56 s | Show **Ready to export** and the reconciled CSV, report, and manifest actions. | “Trusted output stays gated until review and required value decisions are complete.” | End on product screen. |

Rehearse the checked-in public sample from **Try sample data → File details → Match setup → Review matches → Merge values → Export**. Record the five stages of one normal run, then edit around the matching wait and manual value-resolution time without implying that the sample CTA decided identity, confirmed mappings, or applied merge rules. Show genuine current UI states; do not inject screenshots or fabricated outcomes. Keep technical disclosures collapsed. The [README screenshots](assets/readme/) cover the same product stages. This walkthrough is product-flow evidence, not an accuracy claim.
